route:
  receiver: playground-log
  group_by: [alertname, service, environment]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: playground-log
    webhook_configs:
      - url: http://triovexa:8080/webhooks/grafana
        send_resolved: true
